/**
 * Helper util to replace all href/src URLs with
 * agnostic protocols to support both HTTP/HTTPS
 */

const agnostify = (text) => text.replace(/(?!'|")http(s?):\/\//gi, '//')

export default agnostify
