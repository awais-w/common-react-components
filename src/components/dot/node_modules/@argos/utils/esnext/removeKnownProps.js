const removeKnownProps = (PROP_TYPES, props) =>
  Object.keys(props).reduce((prev, key) => {
    if (typeof PROP_TYPES[key] === 'undefined') {
      prev[key] = props[key] // eslint-disable-line no-param-reassign
    }
    return prev
  }, {})

export default removeKnownProps
